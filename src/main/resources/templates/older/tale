<!--
                <a class="firstPage" th:if="${not pageInfo.isFirstPage()}" th:href="@{${'audit'}(page=${pageInfo.getCurrentPage() - 1})}" aria-label="Previous">
                    <span aria-hidden="true">&laquo;上一页</span>
                </a>
                <a class="disabled firstPage" th:if="${pageInfo.isFirstPage()}" href="javascript:void(0);" aria-label="Previous">
                    <span aria-hidden="true">&laquo;首页</span>
                </a>

<li th:each="pageNo : ${#numbers.sequence(1, totalPages)}">
    <a th:if="${pageNum eq pageNo}" href="javascript:void(0);">
        <span th:text="${pageNo}"></span>
    </a>

    <a th:if="${not (pageNum eq pageNo)}" th:href="@{${'center/audit'}(page=${pageNo})}">
        <span th:text="${pageNo}"></span>
    </a>
</li>-->
<!--
                <a class="lastPage" th:if="${not pageInfo.isLastPage()}" th:href="@{${'audit'}(page=${pageInfo.getPageCount()})}" aria-label="Next">
                    <span aria-hidden="true">&raquo;尾页</span>
                </a>
                <a class="lastPage" th:if="${pageInfo.isLastPage()}" href="javascript:void(0);" aria-label="Next">
                    <span aria-hidden="true">&raquo;尾页</span>
                </a>
                -->